# `send-jcode` - Utility to stream JCode to a robot
- This utility allows the streaming of JCode to a device.
    - This means it will ensure there is a maximum amount of instructions with the device at any time.
## Usage
- Install: `$ make install`
- Help: `$ send-jcode -h`
- Run: `$ send-jcode --input <input file>`